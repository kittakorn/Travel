<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:TravelApp.ViewModels;assembly=TravelApp"
             mc:Ignorable="d"
             x:Class="TravelApp.Views.RegisterPage"
             Title="สมัครสมาชิก">
    <ContentPage.BindingContext>
        <viewModels:RegisterViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="อีเมล" Text="{Binding Register.Email}"></Entry>
            <Entry Placeholder="รหัสผ่าน" Text="{Binding Register.Password}"></Entry>
            <Entry Placeholder="ยืนยันรหัสผ่าน" Text="{Binding Register.ConfirmPassword}"></Entry>
            <Entry Placeholder="ชื่อที่แสดง" Text="{Binding Register.Name}"></Entry>
            <Entry Placeholder="เบอร์โทรศัพท์" Text="{Binding Register.Phone}"></Entry>
            <Button Text="สมัครสมาชิก" Command="{Binding RegisterCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>