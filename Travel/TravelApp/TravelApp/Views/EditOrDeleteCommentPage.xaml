<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:TravelApp.ViewModels;assembly=TravelApp"
             mc:Ignorable="d"
             x:Class="TravelApp.Views.EditOrDeleteCommentPage"
             Title="แก้ไขความคิดเห็น"
             Visual="Material">

    <ContentPage.BindingContext>
        <viewModels:EditOrDeleteCommentViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem  Order="Primary" IconImageSource="ic_action_save.png" Priority="1"  Command="{Binding PutCommentCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Editor Text="{Binding Comment.CommentDescription}" Margin="5" AutoSize="TextChanges" MaxLength="100" TextColor="Green" Placeholder="ข้อความ"></Editor>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>